// define the services
// each service has an optional github repo
// each service has an optional local path
// each service has an optional branch
// each service has a name
// each service has a container name
// each service has an optional dict of env vars
