// define the services
// each service has a path to the DockerFile
// each service has a path to the docker-compose file to be run
// each service can have an optional list of environment variables to be mapped for the running of service

